object JumpGame4 {

  def minJumps(arr: Array[Int]): Int = {
    val grph:Map[Int, List[Int]] = Map()

    for(i <- 0 to arr.length-1){
        
    }

    return 0
  }
  def main(args: Array[String]):Unit = {
      minJumps(Array(1,2,3,4,5))
  }
}
